# IntelliPlant backend — core dependencies (pure-python friendly, Python 3.12+)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sqlalchemy>=2.0
PyJWT>=2.8
pypdf>=4.0
openpyxl>=3.1
anthropic>=0.40

# Optional upgrades (enable automatically if installed):
#   pip install -r requirements-full.txt
# chromadb            -> replaces built-in vector store
# sentence-transformers -> replaces built-in hashing embeddings
